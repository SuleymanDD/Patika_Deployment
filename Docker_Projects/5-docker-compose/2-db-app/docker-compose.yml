version: "3.4"
services: 
  db-app:
    container_name: db-app
    build: .
    ports: 
     - 3000:3000
  mongo-db:
    container_name: mongo-db
    image: mongo
    ports: 
     - 27017:27017
    volumes:
     - db-app-data:/data/db
volumes: 
  db-app-data: